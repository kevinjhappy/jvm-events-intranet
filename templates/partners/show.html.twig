{% extends 'base.html.twig' %}

{% block title %}Partners{% endblock %}

{% block body %}
    <h1>Partners</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partner.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ partner.name }}</td>
            </tr>
            <tr>
                <th>Equipment</th>
                <td>{{ partner.equipment }}</td>
            </tr>
            <tr>
                <th>Address</th>
                <td>{{ partner.address }}</td>
            </tr>
            <tr>
                <th>HelpingArea</th>
                <td>{{ partner.helpingArea }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partners_index') }}">back to list</a>

    <a href="{{ path('partners_edit', {'id': partner.id}) }}">edit</a>

    {{ include('partners/_delete_form.html.twig') }}
{% endblock %}
